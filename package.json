{
	"name": "amalthea",
	"version": "1.0.0",
	"main": "index.js",
	"repository": "https://github.com/Jinglue/Amalthea.git",
	"author": "Apollo <zhaoyang@jizhi.im>",
	"license": "MIT",
	"scripts": {
		"test": "jest && eslint .",
		"hashmark": "hashmark dist/*.js -rsl 7 'dist/amalthea.{hash}.{name}.js'",
		"build": "rm -rf dist && coffee -cbm src/main.coffee && r.js -o src/build.js baseUrl=./src name=almond include=main out=dist/bundle.js && hashmark dist/*.js -rsl 7 'dist/amalthea.{hash}.{name}.js' && gzip -r -f -k dist/*.js"
	},
	"devDependencies": {},
	"dependencies": {
		"coffee": "^4.1.0",
		"hashmark": "^5.0.0",
		"r.js": "^1.0.3"
	}
}
